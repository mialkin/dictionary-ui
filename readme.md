# Dictionary UI

```bash
# Run application using Node.js:
npm run dev # Open http://localhost:3000 in your browser

# Alternatively run application in Docker:
docker run -p 3000:3000 mialkin/dictionary-ui:1.0.0
```
